#include <stdio.h>

/**
 * main - addresses of variables
 *
 * Return: Always 0
 */
int main(void)
{
	char c;
	int i;

	printf("Address of variable 'c' is %p\n", &c);
	printf("Address of variable 'i' is %p\n", &i);

	return (0);
}
